{
  "name": "noorkids-app",
  "private": true,
  "description": "NoorKids App - Root workspace. Run client and server separately.",
  "scripts": {
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev"
  }
}
